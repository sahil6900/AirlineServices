server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/UserEntity
    username: root
    password: 112296root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
   